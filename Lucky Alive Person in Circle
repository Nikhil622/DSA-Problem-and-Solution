class Solution{
public:
    int find(int n){
       deque<int>q;
       for(int i=1;i<=n;++i)
         q.push_back(i);
       while(q.size()>2)
       {  int x=q.front();
          q.pop_front();
          q.pop_front();
          q.push_back(x);
       }
       return q.front();
    }
};
