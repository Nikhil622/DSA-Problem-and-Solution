int max_sum(int a[],int n)
{  int sum=0;
   int curr=0;
   for(int i=0;i<n;++i)
   { sum+=a[i];
     curr+=(a[i]*i);
   }
   int res=curr;
   for(int i=1;i<n;++i)
   {  curr=curr+sum-(a[n-i]*n);
      res=max(res,curr); 
   }
   return res;
}
