class Solution{   
public:
    int minxorpair(int N, int arr[]){    
        sort(arr,arr+N);
        int res=INT_MAX;
        for(int i=1;i<N;++i)
         res=min(res,arr[i]^arr[i-1]);
         // Minium xor will be : in between consequetive elements in sorted array
        return res;
    }
};
